# Runtime dependencies (installed into the Lambda zip).
# Pin to ranges that resolve to wheels compatible with the Lambda Python 3.12 runtime.
fastapi==0.115.6
mangum==0.19.0
pydantic==2.10.4
pydantic-settings==2.7.1
boto3==1.35.90
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
python-ulid==3.0.0
email-validator==2.2.0
# Ask Surya AI. Pure-python wheel; its compiled transitives (pydantic-core via
# pydantic, jiter) ship cp312 manylinux2014 wheels, so the CI --only-binary
# build resolves cleanly for the Lambda runtime.
anthropic==0.112.0
